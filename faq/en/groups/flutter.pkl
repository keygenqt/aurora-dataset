import "../../../templates/FaqTemplate.pkl"
import "../../../authors/en.pkl" as authors

flutter = new Listing<FaqTemplate> {
    new {
        t = "What is Flutter CLI?"
        b = """
        This is the Flutter command line interface that provides basic functionality for the framework.
        """
        a = authors.keygenqt
    }
}
